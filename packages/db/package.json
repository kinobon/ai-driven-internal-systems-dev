{
  "name": "@repo/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json --noEmit",
    "lint": "echo 'Linting db package'",
    "test": "echo 'No tests for db package yet'"
  },
  "dependencies": {
    "@repo/core": "workspace:*"
  }
}
