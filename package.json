{
  "name": "ai-driven-internal-systems-dev",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "prepare": "node ./scripts/setupGitHooks.mjs"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "turbo": "^2.0.0",
    "@types/node": "^20.16.10",
    "typescript": "^5.6.3"
  },
  "packageManager": "pnpm@8.15.0",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
